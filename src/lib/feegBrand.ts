/** Archivo en `public/` (`LOGO_FEG 1.svg` → espacio codificado para URL). */
export const FEG_LOGO_PUBLIC_PATH = "/LOGO_FEG%201.svg" as const;

export const FEG_LOGO_MIME = "image/svg+xml" as const;
